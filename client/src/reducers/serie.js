/*
 import {

 } from '../constants/serie';
 */
const initialState = {
  serie: null
};

const serie = (state = initialState, action) => {
  switch (action.type) {
    default:
      return {...state};
  }
};

export default serie;
